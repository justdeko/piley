name: Build, Sign, and Release Android App
on:
  push:
    tags:
      - 'v*'

jobs:
  build_and_release:
    name: Build, sign, and release apk
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up JDK
        uses: actions/setup-java@v2
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Build with Gradle
        run: ./gradlew bundleRelease assembleRelease

      - name: Sign APK
        run: |
          echo "${{ secrets.KEYSTORE }}" > keystore.jks
          jarsigner -verbose -sigalg SHA1withRSA -digestalg SHA1 \
            -keystore keystore.jks \
            -storepass ${{ secrets.KEYSTORE_PASSWORD }} \
            -keypass ${{ secrets.KEY_PASSWORD }} \
            path/to/unsigned.apk alias_name


      - name: Zipalign apk
        run: zipalign -v 4 path/to/unsigned.apk path/to/signed-aligned.apk

      - name: Upload Signed APK
        uses: actions/upload-artifact@v2
        with:
          name: signed-apk
          path: path/to/signed-aligned.apk

  create_release:
    name: Create Release
    needs: build_and_release
    runs-on: ubuntu-latest
    steps:
      - name: Download Signed APK
        uses: actions/download-artifact@v2
        with:
          name: signed-apk
          path: signed-apk

      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            signed-apk/*.apk
